apiVersion: v1
kind: Service
metadata:
  name: demo-backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: demo-backend
